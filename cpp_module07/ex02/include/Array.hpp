#ifndef ARRAY_HPP
#define ARRAY_HPP

template <typename T>

class Array {
 public:
  Array(void);
  Array(**replace parameters **);
  Array(Array const &instance);
  Array &operator=(Array const &rhs);
  ~Array(void);

 private:
};

// Array<T>::

#endif